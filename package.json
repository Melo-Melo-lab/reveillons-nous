{
  "name": "reveillons-nous",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx live-server --port=3000 --open=index.html",
    "build": "npm install --prefix admin && npm run build --prefix admin",
    "start": "node server/index.js"
  }
}
